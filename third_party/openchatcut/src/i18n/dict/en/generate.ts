// EN dictionary (field fragmentation, key = Chinese original text). Data files are exempt from the upper limit of row count.
// Intentionally empty: src/generate/* The original text of the error thrown is all in English (en can be used as is),
// This mechanism only supports Chinese key→English, without en→zh reverse, so there is no registration entry.
export default {} as Record<string, string>;
