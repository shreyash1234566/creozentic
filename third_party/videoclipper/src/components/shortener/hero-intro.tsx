const HeroIntro = () => (
  <div className="max-w-3xl space-y-4">
    <div className="inline-flex items-center rounded-full border px-3 py-1 text-xs font-medium text-muted-foreground">
      New automatic flow
    </div>
    <h1 className="text-3xl font-bold tracking-tight sm:text-4xl">
      Clean up or trim any video automatically
    </h1>
    <p className="text-base text-muted-foreground">
      Upload a clip, choose whether to polish the delivery or craft a one-minute
      highlight, and CE.SDK rebuilds the scene while Gemini handles the
      transcript intelligence.
    </p>
  </div>
);

export default HeroIntro;
